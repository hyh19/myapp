version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: batch-file-rename-assistant
    ports:
      - "8080:80"
    volumes:
      - ./build/web:/usr/share/nginx/html:ro
    restart: unless-stopped

